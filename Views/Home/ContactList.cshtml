@* <h1>Address Book</h1>
  @foreach(var Contact in Model)
  {
    <ul>
    <li>Name: @Contact.GetName()</li>
    <li>Phone Number: @Contact.GetPhoneNumber()</li>
    <li>Address: @Contact.GetAddress()</li>
    </ul>
  }

<p><a href="/">Add another contact</a></p>

<form action="/contact/list/clear" method="post">
  <button type="submit" name="button">Clear All Contacts</button>
</form> *@

<h1>Address Book (all contacts)</h1>
<ul>
  @foreach (var contact in Model)
  {
    <li><a href='/ContactList/@contact.GetId()'>@contact.GetName()</a></li>
  }
</ul>
<a href='/contact/create'>Add new contact</a>
